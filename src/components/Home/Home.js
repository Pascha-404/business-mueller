import React, { Fragment } from 'react';
import HeroSection from '../HeroSection';

// Gathers all Sections to display Homeview in main section
function Home() {
    return <Fragment>
        <HeroSection />
    </Fragment>;
}

export default Home;
